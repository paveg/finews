# Phase 1.5 申し送りメモ

| 項目 | 内容 |
|---|---|
| Date | 2026-05-24 / 更新 2026-05-25 |
| Phase 1 状態 | 本番デプロイ完了、natural cron 動作確認待ち(月曜 6:30 JST) |
| 関連 | [設計書](2026-05-23-finews-design.md) / [Phase 1 計画](../plans/2026-05-23-finews-phase-1.md) / [ADR](../../adr/) |

## Phase 1 の到達点

- `daily` ジョブ(半導体 1 領域)が RSS → Stage 1 (Haiku 4.5) → Stage 2 (Sonnet 4.6) → Discord 1 embed で動作
- D1 に `articles` + `deliveries` を蓄積、`deliveries` に tokens + cost を記録
- `BudgetTracker` による多層コスト防御 (ADR-0006)
- 4 RSS ソース: FRB / BOJ / Nikkei xTech / BBC Business
- TDD: `dedup`(6 tests)+ `budget-guard`(6 tests)= 12 tests

## 2026-05-25 セッション進捗(本番デプロイ + リハ試行)

### 完了したこと

- GitHub repo 作成(`paveg/finews` public、gh-infra PR #32 経由)
- Cloudflare Workers Builds 連携(GitHub push で自動 deploy)
  - Build command: `pnpm run build`(= `pnpm -r typecheck`)
  - Deploy command: `pnpm run deploy`
- 本番デプロイ済(`https://finews.pavegy.workers.dev`、最新 commit `4f0f125`)
- secrets 設定済(ANTHROPIC_API_KEY / DISCORD_WEBHOOK_URL / RUN_SECRET)
- `[observability] enabled = true` で persistent Workers Logs 有効
- `ENVIRONMENT = "production"` に修正(以前 development が production に漏れていた)
- Dependabot esbuild CVE 解消(`pnpm overrides` で `>=0.25.0`)

### 判明した制約

- **fetch handler の wall time 30秒固定** で `/__run-daily?secret=...` 経由のリハは Stage 2 まで完走しない。これは Cloudflare の hard limit。memory: [[cf-workers-fetch-vs-scheduled-wall-time]]
- 本番動作確認は **scheduled handler(自然 cron)経由でしかできない**。月曜 6:30 JST の自然発火で確認すること

### Stage 1 出力の修正履歴(LLM の癖補正)

| 問題 | 対策 commit | 教訓 |
|---|---|---|
| `headline_ja` 欠落 | Tool Use 化(`e49c66a`) | `required` 強制で解決 |
| `rationale` length 超過 | Valibot 緩める + slice(`8d943ad`) | `maxLength` は LLM に効かない |
| `tickers: "9020.T"`(scalar) | `ensureArray` 正規化(`a113d30` + `4f0f125`) | `type: array` も hint レベル |

参考: [[anthropic-tool-use-soft-hints]]

### 月曜朝 6:30 JST の確認手順

1. **Cloudflare Dashboard → finews → Logs タブ** を開く(persistent)
2. cron 発火直前に tail 起動(任意): `pnpm wrangler tail`
3. 期待ログ:
   ```
   { cron: '30 21 * * SUN-THU', time: '2026-05-25T21:30:...' }
   { job: 'daily', articlesFetched: N, fresh: M, extracted: K,
     stage2Input: K2, durationMs: 30000-60000,
     budget: { stage1Calls: ..., stage2Calls: 1, costUsdMicro: ... } }
   ```
4. **Discord に半導体ダイジェスト 1 通が届けば Phase 1 完了**
5. 届かない / エラー → Logs タブで `console.warn` / `console.error` 探す

### 残った浪費なしの動作確認手段

- **ローカル fixture test**: `ANTHROPIC_API_KEY=... pnpm test test/summarizer/stage1.test.ts`
  - 3 件の fixture(NVDA / FOMC / BOJ)で Stage 1 動作確認
  - Tool Use + ensureArray + slice 修正が効くか実測
  - コスト: $0.01 程度

### `/__run-daily` の扱い

- 残しているが、**叩いても 30 秒で cancel する**ことが確定
- 削除 or disable は次セッション判断
- もし削除する場合: `apps/worker/src/index.ts` の `fetch` ハンドラ削除 + `Env` から `RUN_SECRET` 削除 + `wrangler secret delete RUN_SECRET`

## Phase 1.5 で必須対応

### 1. 残り 3 領域への拡張 + Discord 4 メッセージ分割 (ADR-0003)

- `jobs/daily.ts` でドメインごとに Stage 2 を 4 回**直列**呼び出し
- prompt caching を効かせるため、システムプロンプトは `cache_control: ephemeral` 固定済み (Phase 1 で実装)
- `notifier/discord.ts` の `sendDailyEmbed` を 4 ドメイン分順次送信、2 通目以降は `flags: 4096` (SUPPRESS_NOTIFICATIONS)、250ms 間隔

### 2. 市場指標フェッチャの代替ソース選定

Phase 1 で Yahoo Finance `query1` エンドポイントが 401(`scripts/verify-sources.sh` で確認)。代替候補:

| ソース | 無料枠 | カバー | 候補度 |
|---|---|---|---|
| **Stooq** | 無制限(CSV) | 米日株、ETF、外為、コモディティ、債券利回り | ◎ |
| **Finnhub** | 60 req/min | 米株中心 + 一部マクロ指標 | ○ |
| **Alpha Vantage** | 25 req/day | 米国インデックス + 外為 | △ (制限厳しい) |
| **Polygon** | 5 req/min | 米株中心(マクロ薄) | △ |

**推奨**: **Stooq 単独**(`stooq.com/q/l/?s=^spx,^vix,^tnx&f=sd2t2ohlcv&h&e=csv` のような CSV、認証不要、レート制限緩い)。Finnhub は補助。

ADR 化: Phase 1.5 着手時に **ADR-0007: 市場指標代替ソース** を起こす。

### 3. ETF フローデータ実装 (ADR-0004)

ADR-0004 の通り iShares CSV(SOXX/TLT/HYG)+ Global X CSV(AIQ/BOTZ)で 5 銘柄。SMH は Phase 2、SPY/QQQ も Phase 2。

**先に検証**: 実 Workers 環境から iShares / Global X CSV が取得できるか(ローカル curl では Bot 検出で 403 だった可能性、Workers IP は別の挙動の可能性)。Task 14 デプロイ後の早期テストで確認。

### 4. Watchlist alias マッチング

- `config/watchlist.ts` を `{ ticker, aliases }[]` 型に変更
- `matchers/watchlist.ts` に正規化ロジック(Nvidia → NVDA、エヌビディア → NVDA、ｴﾇﾋﾞﾃﾞｨｱ → NVDA など)
- Stage 1 プロンプトは既に `ticker_aliases_used` を抽出するので、それと alias 辞書を突合

### 5. 継続テーマスコア計算

- `matchers/continuing-theme.ts` 新設
- 過去 3 日の同領域同 ticker / indicator の出現頻度をスコア化(0-100)
- `articles.continuingThemeScore` を更新
- Stage 2 プロンプトに `継続テーマフラグ` を渡す

### 6. weekly / monthly ジョブ

- **weekly** (土曜 8:30 JST = `30 23 * * 5` UTC): 過去 7 日 summaries + glossary 集計を Stage 2 Weekly (Sonnet 4.6) で要約 → Discord 1 embed
- **monthly** (月初 9:00 JST = `0 0 1 * *` UTC): 過去 30 日 summaries + glossary を Stage 2 Monthly (Opus 4.7) で振り返り → Discord 1 embed

`wrangler.toml` の `triggers.crons` に 2 つ追加。

### 7. Stage 1 を Gemini 2.5 Flash に切替検証 (ADR-0002)

- `summarizer/stage1_gemini.ts` 実装(同一 Valibot スキーマ、同一プロンプト)
- 過去 7 日の articles を両プロバイダで並列実行、計測:
  - Valibot 検証通過率 ≥ 95% → 合格
  - `significance` の一致率 ≥ 80% → 合格
- 合格すれば `STAGE1_PROVIDER=gemini` で切替(月コスト $3 → $0.5 程度)

### 8. 休場日処理

- `config/holidays.ts` に米日固定リスト(数十日/年)
- `jobs/daily.ts` 冒頭で `isMarketHoliday(today, 'us' | 'jp')` チェックして early-return
- Cron 自体は発火、handler 内で skip + `deliveries.status = 'skipped'` 記録

### 9. テスト追加(設計書 §13 Phase 1.5 必須)

- `discord_notifier` の embed 長計算と分割ロジック(6000 字以内に収まる)
- `continuing_theme` スコア計算
- `watchlist` alias マッチング
- `holidays` 判定

## Phase 1 で発見した未解決事項

| 項目 | 詳細 | 対処時期 |
|---|---|---|
| TDnet 公式 RSS なし | `release.tdnet.info` は 403 | Phase 2(I-list HTML スクレイピング検討) |
| Reuters 401 | bot 検出 or 認証必須化 | BBC + 国内系で代替確定(Phase 1.5 で復活見込み低) |
| Yahoo Finance 401 | 同上、API 制限 | Phase 1.5 #2 で Stooq 等に代替 |
| Discord SUPPRESS_NOTIFICATIONS flag 4096 | 現役性未確認 | Phase 1.5 #1 実装時に動作確認 |
| Workers から外部 CSV 取得可否 | iShares / Global X が Workers IP で 403 にならないか | Phase 1.5 #3 実装時に確認 |
| budget guard の maxAttempts=0 ガード欠落 | `withRetry` でリミット 0 渡すと `lastErr` が undefined のまま throw | Phase 1.5 で 1 行追加(現実的に発生しない) |

## コスト試算 (Phase 1.5 ベース)

| 項目 | 月額 |
|---|---|
| Stage 1 Haiku × 22日 × 25記事 | $3.0 |
| Stage 2 Daily Sonnet × 22日 × 4ドメイン(70% cache hit) | $4.5 |
| Stage 2 Weekly Sonnet × 4回 | $2.5 |
| Stage 2 Monthly Opus × 1回 | $0.5 |
| Anthropic 小計 | 約 $10.5 |
| Cloudflare Workers Paid | $5 |
| D1 / Discord | $0 |
| **総合計** | **約 $15.5/月** |

Stage 1 を Gemini Flash 化した場合: 約 $8/月。

## Phase 1.5 着手時の推奨順序

1. **本番デプロイ + 1 週間運用観察**(Phase 1 完了から最低 1 週間は触らない、運用感触を掴む)
2. データ蓄積結果(`SELECT * FROM deliveries ORDER BY attempted_at DESC LIMIT 30`)から:
   - トークン使用量の実測値と試算の差分
   - Stage 1 の `significance` 分布
   - `articles` のソース別ヒット率
3. 上記を見て Phase 1.5 のスコープ調整(例えば BBC Business のヒット率が低ければ別ソース追加)
4. ADR-0007(市場指標代替)を起こす
5. 実装計画 `2026-mm-dd-finews-phase-1.5.md` を新規作成
6. subagent-driven-development で実装

## 設計上、変更しない判断

Phase 1.5 で「触りたくなる」が、Phase 1 の判断を維持すべきもの:

- **D1 + Drizzle 採用**(ADR-0001): Vectorize の導入は Phase 2 以降、データ蓄積が育ってから
- **Anthropic 単独**(ADR-0002): OpenAI 切替は不要、Gemini は Stage 1 のみ検討
- **Cron Trigger 直叩き**(ADR-0005): Queues/Workflows は CPU 14 分超過するまで導入しない
- **保有銘柄を持たない**: 二重管理回避、watchlist のみ
